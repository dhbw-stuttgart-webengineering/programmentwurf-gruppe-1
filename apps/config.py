"""Config for apps folder."""
from django.apps import AppConfig


class AppsConfig(AppConfig):
    """Config for apps folder."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps'
    label = 'apps'
